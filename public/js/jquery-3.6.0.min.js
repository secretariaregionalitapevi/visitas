/* Local fallback placeholder for jQuery 3.6.0
   NOTE: This is a minimal placeholder. If CDN fails and you need full jQuery functionality offline,
   download the official jquery-3.6.0.min.js into this file. */
(function(window){
  if (window.jQuery) return;
  var $ = function(selector){ return document.querySelectorAll(selector); };
  window.jQuery = window.$ = $;
  console.warn('Using placeholder lightweight jQuery shim. For full features, replace public/js/jquery-3.6.0.min.js with the real library.');
})(window);
