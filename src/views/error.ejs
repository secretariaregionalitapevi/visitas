<!DOCTYPE html>
<html lang="pt-br">
    <style>
        /* ==============================================
   PÁGINA DE ERRO - ESTILOS ESPECÍFICOS
============================================== */

/* Container principal do erro */
.error-container {
    min-height: 60vh;
    display: flex;
    align-items: center;
    justify-content: center;
    padding: 2rem 0;
}

/* Card de erro */
.error-card {
    background: linear-gradient(145deg, #ffffff, #f8f9fa);
    border-radius: 20px;
    box-shadow: 0 10px 30px rgba(220, 53, 69, 0.1);
    border: 1px solid rgba(220, 53, 69, 0.1);
    padding: 3rem 2rem;
    text-align: center;
    max-width: 600px;
    width: 100%;
    position: relative;
    overflow: hidden;
}

/* Efeito de fundo animado */
.error-card::before {
    content: '';
    position: absolute;
    top: -50%;
    left: -50%;
    width: 200%;
    height: 200%;
    background: linear-gradient(45deg, transparent, rgba(220, 53, 69, 0.03), transparent);
    animation: errorShimmer 3s ease-in-out infinite;
    z-index: 0;
}

.error-card > * {
    position: relative;
    z-index: 1;
}

@keyframes errorShimmer {
    0% { transform: translateX(-100%) translateY(-100%) rotate(45deg); }
    50% { transform: translateX(100%) translateY(100%) rotate(45deg); }
    100% { transform: translateX(-100%) translateY(-100%) rotate(45deg); }
}

/* Ícone de erro */
.error-icon {
    font-size: 4rem;
    color: #dc3545;
    margin-bottom: 1.5rem;
    animation: errorPulse 2s ease-in-out infinite;
}

@keyframes errorPulse {
    0%, 100% { transform: scale(1); opacity: 1; }
    50% { transform: scale(1.1); opacity: 0.8; }
}

/* Título de erro */
.error-title {
    color: #dc3545;
    font-size: 2rem;
    font-weight: 700;
    margin-bottom: 1rem;
    text-shadow: 0 2px 4px rgba(220, 53, 69, 0.1);
}

/* Mensagem de erro */
.error-message {
    color: #6c757d;
    font-size: 1.1rem;
    line-height: 1.6;
    margin-bottom: 2rem;
    background: rgba(220, 53, 69, 0.05);
    padding: 1rem 1.5rem;
    border-radius: 10px;
    border-left: 4px solid #dc3545;
}

/* Container dos botões */
.error-buttons {
    display: flex;
    gap: 1rem;
    justify-content: center;
    flex-wrap: wrap;
    margin-top: 2rem;
}

/* Botões de ação */
.error-btn {
    padding: 0.75rem 2rem;
    border-radius: 10px;
    font-weight: 600;
    text-decoration: none;
    transition: all 0.3s ease;
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    min-width: 150px;
    justify-content: center;
    border: none;
    cursor: pointer;
}

.error-btn-primary {
    background: linear-gradient(135deg, #1a314a, #0056b3);
    color: white;
    box-shadow: 0 4px 15px rgba(0, 123, 255, 0.3);
}

.error-btn-primary:hover {
    transform: translateY(-2px);
    box-shadow: 0 6px 20px rgba(0, 123, 255, 0.4);
    color: white;
}

.error-btn-secondary {
    background: linear-gradient(135deg, #6c757d, #495057);
    color: white;
    box-shadow: 0 4px 15px rgba(108, 117, 125, 0.3);
}

.error-btn-secondary:hover {
    transform: translateY(-2px);
    box-shadow: 0 6px 20px rgba(108, 117, 125, 0.4);
    color: white;
}

/* Adaptação para header existente */
.dashboard-header-error.error-header {
    background: linear-gradient(135deg, #dc3545, #c82333);
}

.dashboard-header-error.error-header .dashboard-title {
    color: white;
}

.dashboard-header-error.error-header .dashboard-subtitle {
    color: rgba(255, 255, 255, 0.8);
}

.dashboard-header-error.error-header .title-icon {
    color: rgba(255, 255, 255, 0.9);
}

/* Responsividade */
@media (max-width: 768px) {
    .error-card {
        margin: 1rem;
        padding: 2rem 1.5rem;
    }
    
    .error-title {
        font-size: 1.5rem;
    }
    
    .error-icon {
        font-size: 3rem;
    }
    
    .error-buttons {
        flex-direction: column;
        align-items: center;
    }
    
    .error-btn {
        width: 100%;
        max-width: 250px;
    }
}

@media (max-width: 480px) {
    .error-card {
        padding: 1.5rem 1rem;
    }
    
    .error-message {
        font-size: 1rem;
        padding: 0.75rem 1rem;
    }
}

/* Estados de hover para melhor UX */
.error-card:hover {
    transform: translateY(-2px);
    box-shadow: 0 15px 40px rgba(220, 53, 69, 0.15);
}

/* Animação de entrada */
.error-card {
    animation: errorFadeIn 0.6s ease-out;
}

.title-icon-error {
    width: 56px;
    height: 56px;
   
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 24px;
    color: white;
 
}

@keyframes errorFadeIn {
    from {
        opacity: 0;
        transform: translateY(30px);
    }
    to {
        opacity: 1;
        transform: translateY(0);
    }
}

    </style>
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, user-scalable=no">
    <meta name="theme-color" content="#dc3545">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="apple-mobile-web-app-status-bar-style" content="default">
    <meta name="apple-mobile-web-app-title" content="CCB Visits">
    
    <title>Ops...</title>
    
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <!-- Font Awesome -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <link rel="stylesheet" href="/static/css/styles.css">
    <link rel="stylesheet" href="/static/css/error.css">
</head>

<%- include('head') %>

<body class="private-body">
    <!-- Header da Área Privada -->
    <%- include('main_header') %>
    
    <!-- Conteúdo Principal -->
    <main class="main-content">
        <div class="content-wrapper">
            <div class="header-card dashboard-header-error error-header">
                <div class="dashboard-title-section">
                    <div class="title-wrapper">
                        <div class="title-icon-error">
                            <i class="fas fa-exclamation-triangle"></i>
                        </div>
                        <div class="title-content">
                            <h2 class="dashboard-title">Oops! Algo deu errado</h2>
                            <p class="dashboard-subtitle">Encontramos um problema ao processar sua solicitação</p>
                        </div>
                    </div>
                </div>
            </div>

            <div class="error-container">
                <div class="error-card">
                    <div class="error-icon">
                        <i class="fas fa-exclamation-circle"></i>
                    </div>
                    
                    <h3 class="error-title">Erro Encontrado</h3>
                    
                    <p class="error-message">
                        <%= message %>
                    </p>
                    
                    <div class="error-buttons">
                        <a href="/" class="error-btn error-btn-primary">
                            <i class="fas fa-home"></i>
                            Voltar ao Início
                        </a>
                        
                        <a href="javascript:history.back()" class="error-btn error-btn-secondary">
                            <i class="fas fa-arrow-left"></i>
                            Página Anterior
                        </a>
                    </div>
                </div>
            </div>
        </div>
    </main>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
